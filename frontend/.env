# Frontend configuration - use environment variable for flexibility
# Use the same host as the frontend to access the backend from browsers on the network
# This allows the browser to make requests to the backend on the same host as the frontend
VITE_API_URL=${FRONTEND_API_URL:-http://192.168.1.241:80/api}
VITE_HOST=${VITE_HOST:-0.0.0.0}
VITE_PORT=${VITE_PORT:-3000}