# Database Configuration
POSTGRES_USER=${POSTGRES_USER:-totalevo_user}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-totalevo_password}
POSTGRES_DB=${POSTGRES_DB:-bar_pos}
DB_HOST=${DB_HOST:-localhost}
DB_PORT=${DB_PORT:-5432}

# Backend Configuration
BACKEND_PORT=${BACKEND_PORT:-3001}
# BACKEND_EXTERNAL_PORT is no longer used since backend is not exposed externally
NODE_ENV=${NODE_ENV:-development}
# Allow internal container communication only - backend is not exposed externally
BACKEND_CORS_ORIGIN=${BACKEND_CORS_ORIGIN:-http://frontend:3000,http://localhost:3000,http://127.0.0.1:3000}

# Frontend Configuration
FRONTEND_PORT=${FRONTEND_PORT:-3000}
FRONTEND_EXTERNAL_PORT=${FRONTEND_EXTERNAL_PORT:-3000}
FRONTEND_API_URL=${FRONTEND_API_URL:-http://backend:3001}

# JWT Secret for token signing and verification
# Generate a secure secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# IMPORTANT: Never use default values like 'your-secret-key', 'secret', or 'dev-secret'
# The secret must be at least 64 characters long
JWT_SECRET=your-generated-secret-here