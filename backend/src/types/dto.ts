[{"./types": "UserResponseDTO - A safe representation of a user for API responses\n * Excludes sensitive fields like password\n */\nexport interface UserResponseDTO {\n  id: number;\n  name: string;\n  username: string;\n  role: string;\n  tokensRevokedAt: Date | null;"}, {"id": "user.id", "name": "user.name", "username": "user.username", "role": "user.role", "tokensRevokedAt": null, "toUserDTOArray(users": "User[]): UserResponseDTO[] {\n  return users.map(toUserDTO);"}]