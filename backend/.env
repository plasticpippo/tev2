# This file is intentionally minimal as environment variables are passed from Docker Compose
# The main configuration comes from the docker-compose.yml file
# This ensures consistent configuration across environments