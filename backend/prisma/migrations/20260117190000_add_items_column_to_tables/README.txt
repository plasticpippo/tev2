Migration to add items column to tables table
============================================

This migration adds the 'items' column of type JSONB to the 'tables' table.
This allows storing order items directly on tables for the table management feature.