# Database Configuration
DATABASE_URL="postgresql://totalevo_user:totalevo_password@localhost:5432/bar_pos"

# Server Configuration
PORT=3001

# Environment Configuration
# Valid values: development, production, test
# - development: Detailed error messages, stack traces, debug logging enabled
# - production: Generic error messages, no stack traces, secure logging
# - test: Test environment with detailed error messages
# Default: development (if not set)
NODE_ENV=development

# For production deployment, change to:
# NODE_ENV=production

# JWT Configuration
JWT_SECRET=your-secret-key-here

# Logging Configuration
# Valid values: error, warn, info, debug
LOG_LEVEL=info
# Enable debug logging (true/false) - only recommended for development
DEBUG_LOGGING=false
