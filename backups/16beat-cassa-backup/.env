# ============================================
# Bar POS Application - Simplified Environment Configuration
# ============================================
# This is the only file you need to modify after cloning the repository

# ============================================
# Network Configuration
# ============================================
# The URL where the application will be accessible
# Examples: http://example.local, http://192.168.1.241, http://localhost
URL=http://192.168.1.241

# Port for nginx (the main entry point)
NGINX_PORT=80

# ============================================
# Docker Port Exposure (Development)
# ============================================
# These settings control port exposure via docker-compose.override.yml
# The override file is automatically merged for local development.
#
# For production: Delete/rename docker-compose.override.yml or use:
#   docker compose -f docker-compose.yml up -d
#
# EXPOSE_DB_PORT: Set to true to expose database port (for pgAdmin, DBeaver, etc.)
EXPOSE_DB_PORT=true

# EXPOSE_FRONTEND_PORT: Set to true to expose frontend port (direct access, bypasses nginx)
EXPOSE_FRONTEND_PORT=false

# Optional: Custom ports (defaults used if not set)
# DB_PORT=5432
# FRONTEND_PORT=3000

# ============================================
# Database Configuration
# ============================================
POSTGRES_USER=totalevo_user
POSTGRES_PASSWORD=totalevo_password
POSTGRES_DB=bar_pos

# ============================================
# JWT Secret for token signing and verification
# ============================================
# IMPORTANT: Generate a secure random secret for production use
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-super-mega-sercret-jwt-code-change-this-paramenter!!!-43901hdiqii3hbkfsakfg
